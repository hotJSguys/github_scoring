const GITHUB_ENDPOINT = 'https://api.github.com';

class GithubServices {
  async getData() {
    try {
      return null;
    } catch (error) {
      console.log(error.message);
    }
  }
}

export default CLASS_SERVICES;
